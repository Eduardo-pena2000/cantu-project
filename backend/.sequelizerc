const { resolve } = require("path");

module.exports = {
  config: resolve("src", "infraestructure", "config", "database", "config.js"),
  "seeders-path": resolve("src", "infraestructure", "database", "seeders"),
  "migrations-path": resolve("src", "infraestructure", "database", "migrations"),
  "models-path": resolve("src", "infraestructure", "database", "models"),
};
