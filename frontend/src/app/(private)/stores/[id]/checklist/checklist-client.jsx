"use client";

import React from "react";

export function ChecklistClient({ store }) {
  return (
    <div>
      <h1>Checklist para {store?.name}</h1>
      <p>Componente en construcción.</p>
    </div>
  );
}
